services:
  - type: web
    name: searchai
    runtime: docker
    region: oregon
    plan: free
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
